#include "config.h"
#include "wifi_init.h"

void setup() {
  Serial.begin(115200);
  delay(10);
  if (conectaWiFi()){
    
     
  }

}

void loop() {
  // put your main code here, to run repeatedly:

}
